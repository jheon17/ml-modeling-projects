# 중고차 가격 예측

## 프로젝트 목적

차량 정보를 이용해 중고차 판매 가격을 예측하는 회귀 모델링 과정을 학습하는 프로젝트입니다.

단순히 모델을 실행하는 것보다 다음 과정을 직접 확인하고 기록하는 것을 목적으로 합니다.

- 원본 데이터 구조와 품질 확인
- 회귀 문제에 필요한 전처리
- 기준 모델(Baseline) 설정
- 회귀 모델 학습 및 평가
- 모델별 결과 비교
- 예측 오차 분석

아직 수행하지 않은 분석이나 모델링 결과는 작성하지 않습니다.

## 데이터

이번 프로젝트에서는 Kaggle의 `Vehicle dataset from CarDekho` 데이터셋에 포함된 아래 원본 파일을 사용합니다.

`Car details v3.csv`

데이터 출처:
https://www.kaggle.com/datasets/nehalbirla/vehicle-dataset-from-cardekho

원본 데이터는 다음 위치에 로컬로 보관합니다.

`data/raw/Car details v3.csv`

원본 CSV는 Git 저장소에 포함하지 않습니다.

## 데이터 구조 확인

`01_data_check.ipynb`에서 원본 데이터를 불러와 기본 구조를 확인했습니다.

- 데이터 크기: 8,128행 × 13열
- 예측 대상(Target): `selling_price`
- 데이터 구조 확인 과정에서 오류 없음

확인된 컬럼은 다음과 같습니다.

- `name`
- `year`
- `selling_price`
- `km_driven`
- `fuel`
- `seller_type`
- `transmission`
- `owner`
- `mileage`
- `engine`
- `max_power`
- `torque`
- `seats`

현재 데이터 타입은 다음과 같이 확인되었습니다.

- 정수형: `year`, `selling_price`, `km_driven`
- 실수형: `seats`
- 문자열형: `name`, `fuel`, `seller_type`, `transmission`, `owner`, `mileage`, `engine`, `max_power`, `torque`

`mileage`, `engine`, `max_power`에는 숫자와 단위가 함께 문자열로 저장되어 있습니다.

예시:

- `mileage`: `23.4 kmpl`
- `engine`: `1248 CC`
- `max_power`: `74 bhp`

`torque`는 다음과 같이 여러 표기 형태가 존재하는 것을 확인했습니다.

- `190Nm@ 2000rpm`
- `250Nm@ 1500-2500rpm`
- `12.7@ 2,700(kgm@ rpm)`
- `22.4 kgm at 1750-2750rpm`

따라서 문자열 컬럼의 전처리 방법은 추가 확인 후 결정할 예정입니다.

### 결측치

결측치가 확인된 컬럼은 다음과 같습니다.

| 컬럼 | 결측치 수 |
|---|---:|
| `mileage` | 221 |
| `engine` | 221 |
| `max_power` | 215 |
| `torque` | 222 |
| `seats` | 221 |

그 외 현재 확인한 컬럼에서는 결측치가 발견되지 않았습니다.

이 초기 확인 단계에서는 결측치 삭제·대체, 데이터 타입 변환, 문자열 정리 등의 전처리를 수행하지 않았습니다.

### 데이터 품질 추가 확인

전처리 방법을 결정하기 전에 중복 행, 범주형 변수의 고유값, 결측치가 같은 행에서 겹치는 패턴을 추가로 확인했습니다.

#### 중복 행

13개 컬럼의 값이 모두 동일한 중복 행은 `1,202`개로 확인되었습니다.

중복 구조를 추가로 확인한 결과:

- 중복 제거 전 전체 데이터: `8,128`행
- 중복 제거 시 남는 고유 행: `6,926`행
- 반복되는 동일 행 그룹: `625`개
- 한 동일 행 그룹의 최대 반복 횟수: `34`회

대부분의 중복 그룹은 2회 등장했지만, 일부 동일 기록은 29~34회 반복되었습니다.

개별 차량을 식별할 수 있는 별도의 ID가 없어 동일한 조건의 서로 다른 차량인지 단순 반복 기록인지 완전히 구분할 수는 없습니다. 다만 동일한 13개 컬럼 값이 여러 차례 반복되어 있어, 학습·평가 데이터 분리 시 동일 기록이 양쪽에 포함되어 평가 결과가 왜곡될 가능성을 줄이기 위해 완전 중복은 한 건만 유지하기로 결정했습니다.

`02_preprocessing.ipynb`에서 원본 데이터는 유지한 채 완전 중복을 제거한 별도 DataFrame을 생성했습니다.

처리 결과:

- 처리 전: `8,128`행
- 제거된 완전 중복: `1,202`행
- 처리 후: `6,926`행
- 처리 후 남은 완전 중복: `0`행

원본 CSV는 수정하지 않았습니다.

#### 범주형 변수

확인된 범주형 변수의 고유값은 다음과 같습니다.

- `fuel`: 4개
  - `Diesel`
  - `Petrol`
  - `LPG`
  - `CNG`
- `seller_type`: 3개
  - `Individual`
  - `Dealer`
  - `Trustmark Dealer`
- `transmission`: 2개
  - `Manual`
  - `Automatic`
- `owner`: 5개
  - `First Owner`
  - `Second Owner`
  - `Third Owner`
  - `Fourth & Above Owner`
  - `Test Drive Car`

아직 범주형 변수의 인코딩은 수행하지 않았습니다.

#### 결측치 겹침

결측치가 존재하는 다음 5개 컬럼을 기준으로 행별 결측치 개수를 확인했습니다.

- `mileage`
- `engine`
- `max_power`
- `torque`
- `seats`

확인 결과:

| 한 행에서 결측인 컬럼 수 | 행 수 |
|---:|---:|
| 0개 | 7,906 |
| 1개 | 1 |
| 2개 | 0 |
| 3개 | 0 |
| 4개 | 6 |
| 5개 | 215 |

이 중 `215`개 행에서는 위 5개 컬럼이 모두 동시에 결측인 것으로 확인되었습니다.

현재는 결측치를 삭제하거나 대체하지 않았습니다. 결측치가 집중된 행의 특성을 추가로 확인한 뒤 처리 방법을 결정할 예정입니다.

#### 중복 제거 후 결측치 재확인

완전 중복을 제거한 `6,926`행을 기준으로 결측치 현황을 다시 확인했습니다.

컬럼별 결측치 수는 다음과 같습니다.

| 컬럼 | 결측치 수 |
|---|---:|
| `mileage` | 208 |
| `engine` | 208 |
| `max_power` | 205 |
| `torque` | 209 |
| `seats` | 208 |

위 5개 컬럼을 기준으로 행별 결측치 개수를 확인한 결과:

| 한 행에서 결측인 컬럼 수 | 행 수 |
|---:|---:|
| 0개 | 6,717 |
| 1개 | 1 |
| 4개 | 3 |
| 5개 | 205 |

5개 컬럼 중 하나라도 결측인 행은 총 `209`행이며, 이 중 `205`행은 5개 컬럼이 모두 동시에 결측입니다.

정확히 4개 컬럼이 결측인 3개 행에서는 `max_power`만 값이 존재했고, 세 행 모두 값이 `0`으로 확인되었습니다.

정확히 1개 컬럼만 결측인 1개 행은 `torque`가 결측이었으며, 해당 행의 `max_power` 값은 `bhp`로 확인되었습니다.

`0`이나 `bhp`처럼 결측치는 아니지만 실제 수치로 바로 사용할 수 있는지 추가 확인이 필요한 문자열이 존재하므로, 현재 단계에서는 결측 행의 삭제·대체 기준을 확정하지 않았습니다.

#### 차량 제원 문자열 형식 확인

숫자 개념이 문자열로 저장된 `mileage`, `engine`, `max_power`, `torque` 컬럼의 실제 표기 형식을 확인했습니다.

이 형식 확인 단계에서는 문자열을 숫자형으로 변환하거나 값을 수정하지 않았습니다.

##### `mileage`

결측치를 제외한 값에서 앞부분의 숫자와 단위를 확인한 결과 숫자 추출에 실패한 행은 없었습니다.

확인된 단위는 다음과 같습니다.

| 단위 | 행 수 |
|---|---:|
| `kmpl` | 6,631 |
| `km/kg` | 87 |

따라서 `mileage`에는 두 종류의 단위가 존재하는 것을 확인했습니다.

##### `mileage` 단위와 0값 추가 확인

`mileage`를 숫자형으로 변환하기 전에 단위와 연료 유형의 관계를 추가로 확인했습니다.

연료 유형과 단위는 다음과 같이 대응했습니다.

| 연료 | `km/kg` | `kmpl` |
|---|---:|---:|
| CNG | 52 | 0 |
| Diesel | 0 | 3,658 |
| LPG | 35 | 0 |
| Petrol | 0 | 2,973 |

각 연료 유형에서는 한 종류의 mileage 단위만 확인되었습니다.

- CNG, LPG: `km/kg`
- Diesel, Petrol: `kmpl`

결측치를 제외한 `mileage`의 숫자 부분은 모두 정상적으로 추출할 수 있었으며 숫자 변환 실패는 없었습니다.

숫자값의 전체 범위는 다음과 같습니다.

- 개수: `6,718`
- 평균: 약 `19.47`
- 중앙값: `19.44`
- 최솟값: `0.0`
- 최댓값: `42.0`

이 중 `0.0 kmpl` 값이 `15`행 존재했습니다.

15개 행에 대해 동일한 `name`을 가진 다른 차량의 양수 mileage 기록이 존재하는지 확인했지만 비교 가능한 행은 없었습니다. 동일한 `name + year` 조건에서도 양수 mileage 기록은 확인되지 않았습니다.

따라서 데이터 내부에서 해당 15행의 실제 mileage 값을 추정할 근거는 확보하지 못했습니다.

`0.0 kmpl`은 실제 연비값으로 그대로 사용하기보다 유효한 수치로 보기 어려운 값으로 판단했으며, 이후 전처리에서 결측값으로 취급하기로 결정했습니다. 다만 다른 차량의 mileage나 평균·중앙값으로 임의 대체하는 방법은 아직 결정하지 않았습니다.

이 조사 단계에서는 `0.0 kmpl` 값을 실제로 변경하지 않았습니다.

##### `mileage` 전처리 적용

확인한 처리 기준을 바탕으로 `02_preprocessing.ipynb`에서 실제 `mileage` 전처리를 적용했습니다.

완전 중복만 제거된 `df_clean`은 유지하고, 실제 전처리를 적용할 별도 DataFrame인 `df_preprocessed`를 생성했습니다.

`mileage`에서는 문자열 앞부분의 숫자를 추출해 숫자형으로 변환했습니다.

앞서 유효한 연비값으로 보기 어렵다고 판단한 `0.0 kmpl` 15행은 실제 값을 임의로 추정하지 않고 결측값으로 처리했습니다.

처리 결과:

- 데이터 타입: `float64`
- 처리 전 mileage 결측치: `208`행
- 추가로 결측 처리한 `0.0` 값: `15`행
- 처리 후 mileage 결측치: `223`행
- 처리 후 남은 mileage 0값: `0`행
- 숫자값이 남아 있는 mileage: `6,703`행

처리 후 mileage 숫자값의 범위는 다음과 같습니다.

- 평균: 약 `19.51`
- 중앙값: `19.49`
- 최솟값: `9.0`
- 최댓값: `42.0`

`mileage` 이외의 컬럼은 `df_clean`과 동일한 것을 확인했습니다.

또한 처리 후에도 데이터 크기는 `6,926행 × 13열`로 유지되었으며, 새롭게 발생한 완전 중복 행은 없었습니다.

현재 숫자형으로 변환된 mileage에는 연료 유형에 따라 `kmpl`과 `km/kg` 두 단위의 값이 존재합니다.

- Diesel, Petrol: `kmpl`
- CNG, LPG: `km/kg`

이번 단계에서는 두 단위를 임의로 변환하거나 같은 물리 단위라고 가정하지 않았습니다. 단위 차이를 모델링 단계에서 어떻게 반영할지는 추가로 결정할 예정입니다.

##### `engine`

결측치를 제외한 모든 값에서 숫자 부분을 추출할 수 있었습니다.

확인된 단위:

| 단위 | 행 수 |
|---|---:|
| `CC` | 6,718 |

현재 확인한 `engine` 값은 모두 숫자와 `CC` 단위의 조합으로 구성되어 있습니다.

##### `engine` 숫자값 추가 확인 및 처리 기준

`engine`을 실제 숫자형으로 변환하기 전에 숫자값의 범위와 예외 여부를 추가로 확인했습니다.

결측치를 제외한 `6,718`행은 모두 숫자 부분을 정상적으로 추출할 수 있었으며, 숫자 변환 실패는 없었습니다.

숫자값의 분포는 다음과 같습니다.

- 개수: `6,718`
- 평균: 약 `1,430.89`
- 중앙값: `1,248`
- 최솟값: `624`
- 최댓값: `3,604`

0 이하의 engine 값은 확인되지 않았습니다.

가장 작은 양수 engine 고유값은 다음과 같이 확인되었습니다.

- `624`
- `793`
- `796`
- `799`
- `814`
- `909`
- `936`
- `993`
- `995`
- `998`

가장 작은 값들은 Tata Nano, Maruti Celerio, Maruti 800/Alto/Omni, Renault KWID, Hyundai EON 등 여러 차량에서 반복적으로 확인되었습니다.

현재 데이터 내부에서는 이 작은 engine 값들을 명백한 입력 오류로 판단할 근거가 확인되지 않았습니다.

따라서 `engine`은 별도의 예외값 처리 없이 문자열에서 숫자 부분을 추출해 숫자형으로 변환하기로 결정했습니다.

기존 결측치 `208`행은 그대로 결측 상태로 유지하기로 했으며, 이 조사 단계에서는 실제 숫자형 변환을 적용하지 않았습니다.

##### `engine` 전처리 적용

확인한 처리 기준을 바탕으로 `02_preprocessing.ipynb`에서 실제 `engine` 전처리를 적용했습니다.

`engine` 문자열에서 숫자 부분만 추출해 `df_preprocessed["engine"]`에 숫자형으로 저장했습니다.

예:

- `1248 CC` → `1248.0`
- `796 CC` → `796.0`

처리 결과:

- 데이터 타입: `float64`
- 숫자값이 존재하는 engine: `6,718`행
- 기존 engine 결측치: `208`행
- 처리 후 engine 결측치: `208`행
- 처리 후 engine 0 이하 값: `0`행

숫자형 변환 후 분포는 조사 단계와 동일하게 유지되었습니다.

- 평균: 약 `1,430.89`
- 중앙값: `1,248`
- 최솟값: `624`
- 최댓값: `3,604`

숫자 변환 과정에서 추가로 발생한 결측치는 없었습니다.

또한 기존에 전처리한 `mileage` 상태도 그대로 유지되었습니다.

- mileage 결측치: `223`행
- mileage 0값: `0`행

처리 후 데이터 크기는 `6,926행 × 13열`로 유지되었으며, 새롭게 발생한 완전 중복 행은 없었습니다.

이번 단계에서는 `engine`의 기존 결측치 `208`행을 대체하지 않았으며, 다른 컬럼의 값도 수정하지 않았습니다.

##### `max_power`

대부분의 값은 숫자와 `bhp` 단위의 조합이었지만 일부 예외를 확인했습니다.

| 형식 | 행 수 |
|---|---:|
| 숫자 + `bhp` | 6,717 |
| 숫자만 존재 | 3 |
| 숫자 추출 실패 | 1 |

숫자만 존재한 3개 행의 `max_power` 값은 모두 `0`이었습니다.

숫자 추출에 실패한 1개 행의 값은 `bhp`로, 단위만 있고 숫자가 존재하지 않았습니다.

현재 단계에서는 `0`을 잘못된 값이라고 단정하거나 `bhp`를 임의로 결측치로 변환하지 않았습니다.

##### `max_power` 숫자값 추가 확인 및 처리 기준

`max_power`를 실제 숫자형으로 변환하기 전에 숫자값의 범위와 예외값을 추가로 확인했습니다.

결측치를 제외한 문자열에서 숫자 부분을 추출한 결과 숫자값 `6,720`개를 확인했습니다.

숫자값의 분포는 다음과 같습니다.

- 개수: `6,720`
- 평균: 약 `87.73`
- 중앙값: `81.83`
- 최솟값: `0.0`
- 최댓값: `400.0`

원래 `max_power`가 결측이 아니지만 숫자를 추출할 수 없는 값은 1행이었습니다.

해당 값은 다음과 같습니다.

- `Maruti Omni CNG`, 2000년식
- 원본 값: ` bhp`

단위 문자열만 존재하고 숫자가 없으므로 실제 출력값을 확인할 수 없는 값입니다.

또한 숫자값이 `0`인 행은 3개 확인되었습니다.

- `Maruti Alto K10 LXI`, 2011
- `Maruti Swift Dzire VDI Optional`, 2017
- `Tata Indica Vista Quadrajet LS`, 2012

세 차량 모두 동일한 `name + year`를 가진 다른 행에서 양수 `max_power`가 확인되었습니다.

- `Maruti Alto K10 LXI`, 2011: `67.1`
- `Maruti Swift Dzire VDI Optional`, 2017: `74.0`
- `Tata Indica Vista Quadrajet LS`, 2012: `74.0`

따라서 `0`은 실제 최고출력으로 사용하기보다 유효한 수치로 보기 어려운 값으로 판단했습니다.

다만 동일 차량의 다른 행에서 확인한 값을 현재 단계에서 직접 대입하지는 않기로 했습니다. 이후 모델 평가 과정에서 데이터 분리 이전의 다른 행 정보를 이용해 결측값을 채우는 문제를 피하기 위해, 우선 잘못되거나 숫자가 없는 값을 결측값으로 정리하고 결측값 대체 방법은 별도로 결정할 예정입니다.

양수 `max_power` 값 중 가장 작은 고유값은 다음과 같이 확인되었습니다.

- `32.8`
- `34.2`
- `35.0`
- `35.5`
- `37.0`
- `37.48`
- `37.5`
- `38.0`
- `38.4`
- `40.3`

이 중 가장 작은 값들도 Maruti Omni, Ambassador, Maruti 800 등 여러 차량에서 실제 값으로 반복 확인되었습니다. 따라서 양수인 작은 `max_power` 값은 별도로 제거하거나 보정하지 않기로 했습니다.

`max_power`의 처리 기준은 다음과 같이 결정했습니다.

- 정상적인 양수 값: 숫자형으로 변환
- 숫자값 `0`인 3행: 결측값으로 처리
- 숫자가 없는 ` bhp` 1행: 결측값으로 처리
- 기존 결측치 `205`행: 그대로 유지
- 결측값의 실제 대체 방법: 추후 별도 결정

현재 단계에서는 `df_preprocessed["max_power"]` 값을 실제로 변경하지 않았습니다.

##### `max_power` 전처리 적용

확인한 처리 기준을 바탕으로 `02_preprocessing.ipynb`에서 실제 `max_power` 전처리를 적용했습니다.

`max_power` 문자열에서 숫자 부분을 추출해 `df_preprocessed["max_power"]`에 숫자형으로 저장했습니다.

이 과정에서 숫자가 존재하지 않았던 ` bhp` 1행은 결측값으로 변환되었으며, 유효한 출력값으로 보기 어렵다고 판단한 숫자값 `0` 3행도 결측값으로 처리했습니다.

동일한 `name + year`의 다른 행에서 확인한 `67.1`, `74.0`, `74.0` 값을 직접 대입하지는 않았습니다.

처리 결과:

- 데이터 타입: `float64`
- 숫자값이 남아 있는 max_power: `6,717`행
- 기존 max_power 결측치: `205`행
- 숫자가 없던 ` bhp`: `1`행
- 추가로 결측 처리한 0값: `3`행
- 처리 후 max_power 결측치: `209`행
- 처리 후 남은 max_power 0값: `0`행

처리 후 숫자값의 분포는 다음과 같습니다.

- 평균: 약 `87.77`
- 중앙값: `81.83`
- 최솟값: `32.8`
- 최댓값: `400.0`

예외값으로 확인했던 다음 4개 행은 모두 결측값으로 처리된 것을 확인했습니다.

- `Maruti Alto K10 LXI`, 2011
- `Maruti Swift Dzire VDI Optional`, 2017
- `Tata Indica Vista Quadrajet LS`, 2012
- `Maruti Omni CNG`, 2000

기존에 적용한 `mileage`와 `engine` 전처리 상태도 그대로 유지되었습니다.

- mileage 결측치: `223`행
- mileage 0값: `0`행
- engine 결측치: `208`행
- engine 0 이하 값: `0`행

처리 후 데이터 크기는 `6,926행 × 13열`로 유지되었으며, 새롭게 발생한 완전 중복 행은 없었습니다.

이번 단계에서는 `max_power` 결측값을 평균·중앙값이나 동일 차량의 다른 출력값으로 대체하지 않았습니다.

##### `torque`

`torque`는 다른 제원 컬럼보다 표기 방식이 다양했습니다.

결측치를 제외한 `6,717`행을 단위 문자열 기준으로 서로 겹치지 않게 분류한 결과:

| 유형 | 행 수 |
|---|---:|
| `Nm`만 포함 | 6,226 |
| `kgm`만 포함 | 480 |
| `Nm`과 `kgm` 모두 포함 | 1 |
| `Nm`, `kgm` 모두 미포함 | 10 |

`Nm`과 `kgm`을 동시에 포함한 값도 1건 확인되었습니다.

예시:

`380Nm(38.7kgm)@ 2500rpm`

또한 `Nm`과 `kgm` 어느 것도 문자열에 포함되지 않은 값은 다음과 같이 확인되었습니다.

- `210 / 1900`
- `250@ 1250-5000rpm`
- `510@ 1600-2400`
- `110(11.2)@ 4800`

표기 방식에서도 여러 형태가 확인되었습니다.

- `@` 포함: 6,493행
- ` at ` 포함: 212행
- `-` 포함: 2,196행

예를 들어 다음과 같이 단위, 구분자, 회전수 범위 표기가 혼재합니다.

- `190Nm@ 2000rpm`
- `250Nm@ 1500-2500rpm`
- `12.7@ 2,700(kgm@ rpm)`
- `22.4 kgm at 1750-2750rpm`

따라서 `torque`는 단순히 문자열의 첫 번째 숫자만 추출하기보다 단위와 표기 형식을 고려해 전처리 기준을 별도로 결정할 예정입니다.

##### `torque` 세부 형식 추가 확인

`torque`를 숫자형으로 변환하기 전에 토크값의 단위와 rpm 표기 형식을 추가로 확인했습니다.

중복 제거 후 `torque` 상태는 다음과 같습니다.

- 결측치: `209`행
- 값이 존재하는 행: `6,717`행

값이 존재하는 `6,717`행을 `Nm`, `kgm` 문자열 포함 여부에 따라 서로 겹치지 않게 분류한 결과:

| 유형 | 행 수 |
|---|---:|
| `Nm`만 포함 | 6,226 |
| `kgm`만 포함 | 480 |
| `Nm`, `kgm` 모두 포함 | 1 |
| 두 단위 모두 미포함 | 10 |

`Nm`과 `kgm`이 모두 포함된 값은 1행이었습니다.

예:

`380Nm(38.7kgm)@ 2500rpm`

단위가 명시되지 않은 10행에서는 다음 4종류의 문자열이 확인되었습니다.

| 원본 문자열 | 행 수 |
|---|---:|
| `210 / 1900` | 7 |
| `250@ 1250-5000rpm` | 1 |
| `510@ 1600-2400` | 1 |
| `110(11.2)@ 4800` | 1 |

구분자와 rpm 관련 표기도 여러 형태로 존재했습니다.

- `@` 포함: `6,493`행
- ` at ` 포함: `212`행
- `/` 포함: `22`행
- `-` 포함: `2,196`행
- 쉼표 `,` 포함: `327`행
- `rpm` 문자열 포함: `6,698`행

모든 non-null `torque` 문자열에서는 첫 번째 숫자를 추출할 수 있었으며 숫자 추출 실패는 없었습니다.

다만 첫 번째 숫자를 그대로 하나의 토크값으로 사용하는 것은 적절하지 않다고 판단했습니다. 단위가 `Nm`과 `kgm`으로 나뉘고, 괄호 안에 다른 단위의 값이 함께 적힌 문자열도 존재하기 때문입니다.

`Nm`만 포함된 행의 첫 번째 숫자 범위는 다음과 같습니다.

- 개수: `6,226`
- 중앙값: `160`
- 최솟값: `48`
- 최댓값: `789`

`kgm`만 포함된 행의 첫 번째 숫자 범위는 다음과 같습니다.

- 개수: `480`
- 중앙값: `16.3`
- 최솟값: `4.8`
- 최댓값: `190`

특히 `kgm`으로 분류된 문자열에는 첫 번째 숫자가 `110`, `115`, `130`, `145`, `190`처럼 큰 값도 존재합니다. 따라서 `kgm` 문자열 전체에 단순히 동일한 단위 변환식을 적용하기 전에 해당 형식을 추가 확인할 필요가 있습니다.

괄호가 포함된 `torque`는 `379`행, 고유 문자열 기준 `69`종류였습니다.

대표적인 형식은 다음과 같습니다.

- `20.4@ 1400-3400(kgm@ rpm)`
- `12.7@ 2,700(kgm@ rpm)`
- `380Nm(38.7kgm)@ 2500rpm`
- `110(11.2)@ 4800`
- `48@ 3,000+/-500(NM@ rpm)`

문자열에 포함된 숫자 개수도 일정하지 않았습니다.

| 숫자 개수 | 행 수 |
|---:|---:|
| 1개 | 4 |
| 2개 | 4,237 |
| 3개 | 2,425 |
| 4개 | 2 |
| 5개 | 49 |

숫자가 3개 이상 포함된 행은 총 `2,476`행입니다.

현재 조사 결과를 바탕으로 `torque`는 다음 요소를 분리해서 처리해야 한다고 판단했습니다.

- 토크값
- 토크 단위 (`Nm`, `kgm`)
- rpm 값 또는 rpm 범위
- 단위가 없는 예외 형식
- 괄호 안에 다른 단위나 추가 숫자가 포함된 형식

따라서 현재 단계에서는 `torque`의 첫 번째 숫자를 그대로 최종 변수로 사용하거나 `kgm` 값을 일괄적으로 `Nm`으로 변환하지 않았습니다. 실제 전처리 규칙은 예외 형식을 추가 확인한 뒤 결정할 예정입니다.

##### 큰 `kgm` 표기값 추가 확인

`kgm only`로 분류된 torque 중 첫 번째 숫자가 큰 값의 형식을 추가로 확인했습니다.

첫 번째 숫자가 `50 이상`인 `kgm only` 값은 총 `23`행이었습니다.

숫자별 행 수는 다음과 같습니다.

| 첫 번째 숫자 | 행 수 |
|---:|---:|
| 51 | 1 |
| 53 | 1 |
| 110 | 1 |
| 115 | 5 |
| 130 | 4 |
| 145 | 10 |
| 190 | 1 |

확인된 원본 문자열은 총 8종류였습니다.

| 원본 문자열 | 행 수 |
|---|---:|
| `145@ 4,100(kgm@ rpm)` | 10 |
| `115@ 2,500(kgm@ rpm)` | 4 |
| `130@ 2500(kgm@ rpm)` | 4 |
| `115@ 2500(kgm@ rpm)` | 1 |
| `110@ 3,000(kgm@ rpm)` | 1 |
| `190@ 21,800(kgm@ rpm)` | 1 |
| `51@ 1,750-3,000(kgm@ rpm)` | 1 |
| `53@ 2,000-2,750(kgm@ rpm)` | 1 |

이 23행은 모두 `(kgm@ rpm)` 형태의 괄호형 문자열에 포함되어 있었습니다.

반면 첫 번째 숫자가 `50 미만`인 `kgm only` 457행에서는 여러 표기 방식이 함께 존재했습니다.

| 구분 | 50 미만 | 50 이상 |
|---|---:|---:|
| 전체 행 | 457 | 23 |
| 괄호 포함 | 352 | 23 |
| `(kgm@ rpm)` 포함 | 352 | 23 |
| 숫자 뒤 직접 `kgm` 표기 | 25 | 0 |
| `kgm at` 형태 | 80 | 0 |

따라서 큰 값이 특정 괄호형에 집중되어 있다는 패턴은 확인했지만, 50 미만 값에서도 같은 괄호형이 다수 존재하므로 괄호 형식만으로 실제 단위를 확정할 수는 없었습니다.

큰 값에 해당하는 차량은 BMW 5 Series/X5, Mahindra Logan, Tata Sumo, Ford Ikon, Maruti SX4 등으로 확인되었습니다.

동일한 `name` 또는 `name + year`를 가진 다른 행의 torque 값을 비교했지만, 조사 대상과 다른 torque 표기나 `Nm` 표기를 가진 비교 가능한 행은 확인되지 않았습니다.

따라서 동일 차량 기록을 이용해 큰 값의 단위를 직접 교차검증할 근거는 확보하지 못했습니다.

큰 숫자와 동일한 값이 `Nm only`에서도 사용되는지도 확인했습니다.

| 숫자 | 큰 `kgm only` 행 | 동일 숫자의 `Nm only` 행 |
|---:|---:|---:|
| 51 | 1 | 18 |
| 53 | 1 | 0 |
| 110 | 1 | 77 |
| 115 | 5 | 99 |
| 130 | 4 | 39 |
| 145 | 10 | 63 |
| 190 | 1 | 549 |

`110`, `115`, `130`, `145`, `190`은 `Nm only` 데이터에서도 반복적으로 사용되는 숫자였습니다.

하지만 이 결과만으로 `(kgm@ rpm)` 형식의 큰 값을 모두 Nm이라고 확정할 수는 없습니다. `51`, `53` 역시 동일한 괄호형으로 존재하며, 데이터 내부에서 해당 차량의 다른 단위 표기를 직접 비교할 수 없었기 때문입니다.

현재 확인된 사실은 다음과 같습니다.

- 첫 숫자 `50 이상`인 `kgm only` 23행은 모두 `(kgm@ rpm)` 괄호형에 존재
- 일부 큰 숫자는 `Nm only`에서도 반복적으로 사용
- 동일 차량의 다른 torque 표기는 없어 직접 교차검증 불가
- `50 이상`이라는 기준만으로 `kgm → Nm` 변환 여부를 결정할 근거는 부족

따라서 현재 단계에서는 큰 값을 실제 `kgm`이라고 가정해 단위 변환하거나, 반대로 모두 `Nm`이라고 재분류하지 않았습니다. `(kgm@ rpm)` 형식을 추가로 검증한 뒤 전처리 규칙을 결정할 예정입니다.

##### `torque` Nm 후보값 범위 및 극단값 추가 검증

현재까지 확인한 단위 규칙을 실제 `torque` 컬럼에 적용하기 전에, 조사용 Nm 후보값을 생성해 적용 가능한 범위와 극단값을 추가로 확인했습니다.

다음 세 그룹에 대해서만 임시 Nm 후보값을 생성했습니다.

- `Nm only`: 첫 번째 숫자를 Nm 후보값으로 사용
- `kgm only`이면서 첫 번째 숫자가 50 미만: `1 kgf·m = 9.80665 Nm` 기준으로 변환
- `Nm`과 `kgm`이 모두 존재하는 1행: 명시된 첫 번째 Nm 값 사용

큰 `kgm` 23행과 단위가 없는 10행에는 후보값을 부여하지 않았습니다.

이 기준으로 Nm 후보값을 만들 수 있었던 행은 총 `6,684`행이었습니다.

- 전체 데이터 대비: 약 `96.51%`
- non-null torque 대비: 약 `99.51%`

임시 Nm 후보값의 분포는 다음과 같습니다.

- 개수: `6,684`
- 평균: 약 `170.87`
- 중앙값: `160`
- 최솟값: 약 `47.07`
- 최댓값: `789`

`Nm only`와 `kgm < 50 → Nm` 변환 그룹을 비교한 결과 중앙값과 평균 범위는 비슷한 수준으로 확인되었습니다.

| 그룹 | 행 수 | 최솟값 | 중앙값 | 평균 | 최댓값 |
|---|---:|---:|---:|---:|---:|
| `Nm only` | 6,226 | 48.00 | 160.00 | 약 171.41 | 789.00 |
| `kgm < 50 → Nm` | 457 | 약 47.07 | 약 156.91 | 약 163.07 | 약 456.01 |

다만 Nm 후보값의 상단을 추가 확인하는 과정에서 `789Nm@ 2250rpm` 값이 3행 확인되었습니다.

세 행은 모두 `Maruti Zen D`이며 다음 조건이 동일했습니다.

- engine: `1527`
- max_power: `58`
- torque: `789Nm@ 2250rpm`

동일 `name`, 동일 `name + year`, 동일 `engine + max_power` 조합에서는 다른 torque 값을 가진 비교 행이 없어 데이터 내부에서 정확한 값을 복원할 근거는 확인되지 않았습니다.

진단 목적으로 `torque Nm 후보값 / max_power` 비율도 확인했습니다.

전체 후보값의 비율 분포에서 `Maruti Zen D` 3행은 약 `13.60`으로 가장 높았으며, 그다음으로 높은 값은 약 `3.48`이었습니다.

따라서 `789Nm`은 다른 후보값과 비교했을 때 매우 뚜렷한 극단 패턴으로 확인되었습니다.

외부 차량 제원도 교차 확인했지만 자료에 따라 정확한 최대토크 수치에는 차이가 있었습니다. 다만 확인한 외부 자료들은 모두 `789Nm`과는 크게 다른 값을 제시했습니다.

따라서 현재 단계에서는 `789Nm` 3행을 유효한 torque 값으로 사용하기 어렵다고 판단했습니다. 그러나 외부 제원의 특정 값을 직접 대입하지 않고, 실제 전처리 시 결측값으로 처리하기로 결정했습니다.

이에 따라 현재 규칙 기반 Nm 후보값 `6,684`행 중 `789Nm` 3행을 제외한 `6,681`행에서는 이번 검증에서 추가적인 명백한 오류 패턴이 확인되지 않았습니다.

- 전체 데이터 대비 약 `96.46%`
- non-null torque 대비 약 `99.46%`

아직 처리 기준이 확정되지 않은 값은 다음과 같습니다.

- 큰 `kgm` 표기: `23`행
- 단위 없는 torque: `10`행
- 원래 torque 결측치: `209`행
- `789Nm` 오류 의심값: `3`행

현재 단계에서는 이 기준을 실제 `df_preprocessed["torque"]`에 적용하지 않았습니다.

참고한 외부 제원:

- CarDekho, Maruti Suzuki Zen specifications: https://www.cardekho.com/maruti/maruti-zen-specifications.htm
- VirtuaGarage, Maruti Suzuki Zen-D specifications: https://www.virtuagarage.com/maruti/unordered/maruti-suzuki-zen-d/73852/

##### `torque` 최종 처리 규칙 후보 검증

기존 조사와 외부 제원 교차검증 결과를 바탕으로, 실제 `torque` 컬럼에 적용하기 전 최종 Nm 후보값을 생성해 검증했습니다.

최종 처리 후보 규칙은 다음과 같습니다.

- `Nm only`: 첫 번째 숫자를 Nm로 사용
- `kgm only`이면서 첫 번째 숫자 50 미만: `1 kgf·m = 9.80665 Nm` 기준으로 변환
- `Nm`과 `kgm`이 모두 있는 경우: 명시된 Nm 값 사용
- 큰 `kgm` 표기 중 BMW 2행:
  - `51@ 1,750-3,000(kgm@ rpm)` → 약 `500.14 Nm`
  - `53@ 2,000-2,750(kgm@ rpm)` → 약 `519.75 Nm`
- 그 외 큰 `kgm` 표기 21행: 첫 번째 숫자를 Nm로 사용
- 단위가 없는 10행: 첫 번째 숫자를 Nm로 사용
- `789Nm@ 2250rpm` 3행: 유효한 값으로 보기 어려워 결측 처리

기존에 미확정이었던 33행은 이 규칙을 적용한 후보값에서 모두 해석되었습니다.

- BMW 실제 kgm 변환: `2행`
- 첫 번째 숫자를 Nm로 사용하는 행: `31행`
- 미해석 잔여: `0행`

최종 Nm 후보값은 다음과 같이 구성되었습니다.

- 유효 후보값: `6,714행`
- 결측 후보값: `212행`

결측 `212행`은 정확히 다음으로 구성됩니다.

- 원래 torque 결측: `209행`
- `789Nm` 오류 의심값: `3행`

전체 데이터 `6,926행` 기준으로 약 `96.94%`에서 torque Nm 후보값을 확보했습니다.

원래 non-null torque `6,717행`을 기준으로는 약 `99.96%`가 최종 유효 후보값으로 남았습니다.

최종 후보값 분포는 다음과 같습니다.

- count: `6,714`
- mean: 약 `170.68`
- std: 약 `83.59`
- min: 약 `47.07`
- 25%: `110`
- median: `160`
- 75%: 약 `200.06`
- max: `640`

기존 극단값이었던 `789Nm` 3행을 결측 처리한 뒤 후보값의 최댓값은 `640Nm`으로 내려갔습니다.

상위 값에는 Volvo XC90, BMW 6 Series/X7, Mercedes-Benz S/M/GL-Class, Jaguar XF, Audi Q5/Q7 등 고출력 차량이 포함되었고, 하위 값에는 Tata Nano, Maruti 800/Omni 등 소형 차량이 포함되었습니다.

이번 검증에서는 최종 후보값 상·하위 영역에서 추가적인 명백한 이상 패턴은 확인되지 않았습니다.

현재 단계에서는 이 규칙을 아직 `df_preprocessed["torque"]`에 실제 적용하지 않았습니다.

외부 제원 교차검증에서는 대표적인 미확정 문자열을 차량 제원과 비교해 단위 해석을 확인했습니다.

- BMW 5 Series 530d의 `51@ 1,750-3,000(kgm@ rpm)`은 약 500 Nm 제원과 비교해 실제 kgm 표기로 판단
- BMW X5 3.0d의 `53@ 2,000-2,750(kgm@ rpm)`은 약 520 Nm 제원과 비교해 실제 kgm 표기로 판단
- Ford Ikon의 `130@ 2500(kgm@ rpm)`, Mahindra Logan의 `110@ 3,000(kgm@ rpm)`, Maruti SX4의 `145@ 4,100(kgm@ rpm)` 등은 외부 Nm 제원과 비교해 첫 번째 숫자를 Nm 값으로 해석
- Honda Jazz의 `110(11.2)@ 4800`, Skoda Octavia의 `210 / 1900`, Skoda Superb의 `250@ 1250-5000rpm`, Mercedes-Benz M-Class의 `510@ 1600-2400` 등 단위 없는 표기도 외부 Nm 제원과 비교

외부 자료는 데이터의 특정 값을 직접 대입하기 위한 것이 아니라, 문자열에 포함된 숫자의 단위 해석을 확인하기 위한 참고 근거로만 사용했습니다.

##### `torque` Nm 숫자형 변환 실제 적용

검증한 최종 처리 규칙을 `df_preprocessed["torque"]`에 실제 적용했습니다.

기존 문자열 형태의 torque를 Nm 기준 숫자형 변수로 변환했으며, 적용 후 dtype은 `float64`가 되었습니다.

적용한 주요 규칙은 다음과 같습니다.

- 명시적인 `Nm` 표기: 첫 번째 숫자를 Nm로 사용
- `kgm` 표기 중 첫 번째 숫자가 50 미만인 경우: `1 kgf·m = 9.80665 Nm` 기준으로 변환
- Nm과 kgm이 함께 표기된 경우: 명시된 Nm 값 사용
- BMW 5 Series 530d의 `51@ 1,750-3,000(kgm@ rpm)` → 약 `500.14 Nm`
- BMW X5 3.0d의 `53@ 2,000-2,750(kgm@ rpm)` → 약 `519.75 Nm`
- 나머지 큰 `kgm` 표기 21행: 검증 결과에 따라 첫 번째 숫자를 Nm로 사용
- 단위 없는 10행: 검증 결과에 따라 첫 번째 숫자를 Nm로 사용
- `789Nm@ 2250rpm` 3행: 유효한 값으로 사용하기 어려워 결측 처리

적용 결과는 다음과 같습니다.

- 전체 행: `6,926`
- torque 유효값: `6,714`
- torque 결측값: `212`
- dtype: `float64`

결측 `212행`은 다음으로 구성됩니다.

- 기존 torque 결측: `209행`
- `789Nm@ 2250rpm` 오류 의심값: `3행`

변환 후 torque 분포는 다음과 같습니다.

- count: `6,714`
- mean: 약 `170.68`
- std: 약 `83.59`
- min: 약 `47.07`
- 25%: `110`
- median: `160`
- 75%: 약 `200.06`
- max: `640`

특수 처리 결과도 다시 확인했습니다.

- Maruti Zen D의 기존 `789Nm@ 2250rpm` 3행 → 결측
- BMW 5 Series 530d → 약 `500.14 Nm`
- BMW X5 3.0d → 약 `519.75 Nm`
- Skoda Superb `250@ 1250-5000rpm` → `250 Nm`
- Mercedes-Benz M-Class `510@ 1600-2400` → `510 Nm`
- Honda Jazz `110(11.2)@ 4800` → `110 Nm`
- Skoda Octavia `210 / 1900` → `210 Nm`

`torque` 적용 후에도 기존 전처리 결과는 유지되었습니다.

- mileage 결측: `223`
- mileage 0값: `0`
- engine 결측: `208`
- engine 0 이하: `0`
- max_power 결측: `209`
- max_power 0값: `0`
- torque 결측: `212`

전체 shape은 `(6926, 13)`, 완전 중복 행은 `0`으로 유지되었습니다.

원본 정제본 역할의 `df_clean`은 수정하지 않았으며, `df_clean["torque"]`는 기존 문자열 상태를 유지했습니다.

## 개발 환경

현재 확인된 환경은 다음과 같습니다.

- Python 3.13.15
- uv 0.12.9
- pandas 3.0.5
- ipykernel

프로젝트 의존성은 저장소 루트의 `pyproject.toml`과 `uv.lock`으로 관리합니다.

## 현재 진행 상태

현재까지 완료한 작업:

- 프로젝트 최소 폴더 구조 생성
- 원본 데이터 배치
- uv 기반 Python 가상환경 구성
- 원본 CSV Git 제외 설정
- 원본 데이터의 행·열 수, 컬럼명, 데이터 타입, 결측치 확인
- 중복 행, 범주형 고유값, 결측치 겹침 패턴 확인
- 완전 중복 구조 확인 및 처리 기준 결정
- 완전 중복 1,202행 제거 후 6,926행 유지 확인
- 중복 제거 후 결측치 분포 재확인
- 차량 제원 문자열의 숫자·단위·예외 형식 확인
- mileage 단위와 0값 패턴 확인 및 처리 기준 결정
- mileage 숫자형 변환 및 0값 결측 처리 적용
- engine 숫자 범위 및 예외값 확인 후 숫자형 변환 기준 결정
- engine 숫자형 변환 적용 및 결과 검증
- max_power 숫자 범위와 예외값 확인 후 처리 기준 결정
- max_power 숫자형 변환 및 예외값 결측 처리 적용
- torque 단위·괄호·rpm 표기 형식 세부 조사
- torque의 큰 kgm 표기값과 괄호형 패턴 추가 조사
- torque Nm 후보값 범위 및 789Nm 극단값 검증
- torque 최종 Nm 처리 규칙 후보 검증
- torque Nm 숫자형 변환 실제 적용

아직 mileage·engine·max_power·torque·seats 결측값의 처리 방식 결정, name 및 범주형 변수 처리, train/test 분리, 모델링용 전처리 파이프라인 구성, EDA, 모델 학습은 진행하지 않았습니다.
